<?xml version="1.0" encoding="utf-8"?>
<manifest package="org.charmeck.trailofhistory.ranger"
    xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name="org.charmeck.trailofhistory.ranger.EmailPasswordActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="org.charmeck.trailofhistory.ranger.MainActivity" />
        <activity
            android:name="org.charmeck.trailofhistory.ranger.NewPointOfInterestActivity"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name="org.charmeck.trailofhistory.ranger.DetailPointOfInterestActivity"
            android:parentActivityName=".MainActivity" />
        <activity android:name="org.charmeck.trailofhistory.ranger.MapActivity" />

        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_api_key" />
    </application>

</manifest>